# Тесты API Stellar Burgers

Полный набор тестов для REST API https://stellarburgers.education-services.ru/

## 📋 Требования

- Java 11+
- Maven 3.6+
- Allure CLI (опционально, для просмотра отчетов)

## 🏗️ Структура проекта

```
src/test/java/
├── UserCreateTest.java           # Тесты создания пользователя
├── UserLoginTest.java            # Тесты логина пользователя
├── UserEditTest.java             # Тесты изменения данных пользователя
├── OrderCreateTest.java          # Тесты создания заказов
├── OrderListTest.java            # Тесты получения списка заказов
└── ru/stellarburgers/
    ├── config/
    │   └── ApiConfig.java        # Конфигурация API endpoints
    ├── models/
    │   ├── UserCreateAndEditRequest.java
    │   ├── UserLoginResponse.java
    │   └── OrderCreateRequest.java
    └── steps/
        ├── UserSteps.java        # Шаги для работы с пользователями
        └── OrderSteps.java       # Шаги для работы с заказами
```

## 🚀 Запуск тестов

### Запуск всех тестов
```bash
mvn clean test
```

### Запуск конкретного класса тестов
```bash
mvn test -Dtest=UserCreateTest
```

### Запуск конкретного теста
```bash
mvn test -Dtest=UserCreateTest#createNewUser
```

## 📊 Генерация Allure отчета

### Генерация отчета
```bash
mvn allure:report
```

### Просмотр отчета в браузере
```bash
mvn allure:serve
```

## ✅ Тестовое покрытие

### 🔐 Управление пользователями (UserCreateTest)
- ✅ Создание уникального пользователя (200)
- ✅ Попытка создать дублирующегося пользователя (403)
- ✅ Создание без email (403)
- ✅ Создание без password (403)
- ✅ Создание без name (403)

### 🔓 Логин (UserLoginTest)
- ✅ Успешный логин (200)
- ✅ Логин с неверным email (401)
- ✅ Логин с неверным паролем (401)
- ✅ Логин с неверными данными (401)

### ✏️ Редактирование пользователя (UserEditTest)
- ✅ Изменение email с авторизацией (200)
- ✅ Изменение email без авторизации (401)
- ✅ Изменение password с авторизацией (200)
- ✅ Изменение password без авторизации (401)
- ✅ Изменение name с авторизацией (200)
- ✅ Изменение name без авторизации (401)

### 🍔 Создание заказов (OrderCreateTest)
- ✅ Заказ с авторизацией и ингредиентами (200)
- ✅ Заказ без авторизации с ингредиентами (200)
- ✅ Заказ с авторизацией без ингредиентов (400)
- ✅ Заказ с неверными ингредиентами (500)

### 📦 Список заказов (OrderListTest)
- ✅ Получение заказов без авторизации (401)
- ✅ Получение заказов с авторизацией (200)

## 🛠️ Технологический стек

| Компонент | Версия | Назначение |
|-----------|--------|-----------|
| JUnit | 4.13.2 | Фреймворк для модульных тестов |
| RestAssured | 5.3.1 | Библиотека для тестирования REST API |
| Allure | 2.21.0 | Фреймворк для красивых отчетов |
| Gson | 2.10.1 | Парсинг JSON |
| Maven Surefire | 3.0.0-M9 | Запуск тестов |

## 📝 Особенности реализации

### Аннотации Allure
Все тесты снабжены аннотациями для улучшенной отчетности:
- `@DisplayName` - название теста
- `@Description` - описание теста
- `@Step` - отдельные шаги в тесте

### Независимость тестов
- Каждый тест использует уникальные данные (текущее время)
- Тестовые данные создаются перед тестом и удаляются после
- Тесты не зависят друг от друга

### Обработка ошибок
- Проверяются HTTP коды ответов (200, 400, 401, 403, 500)
- Проверяется содержимое JSON ответов
- Безопасная очистка данных после тестов

## 🔑 Пример использования API

```java
// Создание пользователя
UserCreateAndEditRequest userRequest = new UserCreateAndEditRequest(
    "test@example.com", 
    "password123", 
    "Test User"
);
UserSteps userSteps = new UserSteps();
userSteps.userCreate(userRequest)
    .assertThat().statusCode(200)
    .and()
    .body("success", equalTo(true));

// Логин
userSteps.userLogin(userRequest)
    .assertThat().statusCode(200);

// Создание заказа
List<String> ingredients = new ArrayList<>();
ingredients.add("61c0c5a71d1f82001bdaaa6d");
OrderCreateRequest orderRequest = new OrderCreateRequest(ingredients);
OrderSteps orderSteps = new OrderSteps();
orderSteps.orderCreateAfterLogin(userRequest, orderRequest)
    .assertThat().statusCode(200);
```

## 📊 Результаты последнего запуска

```
Tests run: 21, Failures: 0, Errors: 0, Skipped: 0
Time: 38.99 seconds
Status: SUCCESS ✅
```

## 🐛 Решение проблем

### Проблема: Тесты не запускаются
**Решение**: Убедитесь, что установлены Java 11+ и Maven

```bash
java -version
mvn -version
```

### Проблема: API недоступна
**Решение**: Проверьте интернет подключение и доступность сервера

```bash
curl -s https://stellarburgers.education-services.ru/api/ingredients | head
```

### Проблема: Allure отчет не генерируется
**Решение**: Скачайте Allure CLI

```bash
brew install allure  # macOS
# или для других ОС см. https://docs.qameta.io/allure/
```

## 📚 Документация

- [RestAssured Documentation](https://rest-assured.io/)
- [Allure Documentation](https://docs.qameta.io/allure/)
- [JUnit 4 Documentation](https://junit.org/junit4/)

## 📄 Лицензия

Этот проект создан в образовательных целях.

## 👤 Автор

Автоматизированные тесты API Stellar Burgers

---

**Статус**: ✅ Все тесты проходят успешно
